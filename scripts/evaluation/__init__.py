"""Evaluation harness for pytest-wardenbot.

Not part of the published package; lives in `scripts/evaluation/` and runs
against the bundled deterministic suite to measure how named frontier
models perform against the corpus. See `docs/about/evaluation.md` for the
methodology and `run.py` for the entrypoint.
"""
