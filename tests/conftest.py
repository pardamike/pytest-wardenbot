"""Shared pytest fixtures for the plugin's own test suite.

Note: this conftest is for testing *the plugin itself*. End users of
pytest-wardenbot write their own conftest.py to register a `chatbot` fixture
pointing at their actual chatbot.
"""
