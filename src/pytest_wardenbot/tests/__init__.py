"""Shipped tests that users run against their chatbot.

Invoke with:

    pytest --pyargs pytest_wardenbot.tests

Or import specific test functions/files into your project's own test suite.

Every test in this package uses the `chatbot` fixture (defined in
`pytest_wardenbot.plugin`) which users must override in their conftest.py.
"""
